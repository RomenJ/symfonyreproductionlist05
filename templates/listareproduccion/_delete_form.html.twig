<form method="post" action="{{ path('app_listareproduccion_delete', {'id': listareproduccion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listareproduccion.id) }}">
    <button class="btn">Delete</button>
</form>
