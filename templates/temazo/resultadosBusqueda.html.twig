{% extends 'base.html.twig' %}

{% block title %}Temazo index{% endblock %}

{% block body %}
    <h1>Temazo index</h1>

    <h3>  Coincidencias con el título: {{nombre}} </h3> 
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titulo</th>
                <th>Autor</th>
                <th>Duracion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for busquedaConacion in busquedaConaciones %}
            <tr>
                <td>{{ busquedaConacion.id }}</td>
                <td>{{ busquedaConacion.titulo }}</td>
                <td>{{ busquedaConacion.autor }}</td>
                <td>{{ busquedaConacion.duracion }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('app_temazo_show', {'id': busquedaConacion.id}) }}">show</a>
                    <a class="btn btn-primary" href="{{ path('app_temazo_edit', {'id': busquedaConacion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_temazo_new') }}">Create new</a>
{% endblock %}
